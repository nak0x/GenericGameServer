{% extends 'dashboard/dashboard_base.html.twig' %}

{% block content %}
<div class="container">
    <h1 class="title is-1">{{ game.name }}</h1>
    <p class="subtitle">{{ game.slug }}</p>
</div>
{% endblock %}
